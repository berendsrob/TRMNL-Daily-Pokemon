<div class="view">
  <div class="layout layout--col gap--medium">

    <div class="flex flex--row flex--center-y gap--xlarge" style="flex:1 1 auto; min-height:0;">

      <div class="flex flex--col flex--center-x flex--center-y gap--xsmall" style="flex:0 0 52%; height:100%;">
        <img class="image image--contain w--full" src="{{ artwork }}" style="height:100%;" />
        <span class="label text--gray-40">{{ t.number }} {{ dex_padded }}</span>
      </div>

      <div class="flex flex--col gap--small" style="flex:1 1 auto; min-width:0;">

        <span class="value value--large">{{ name }}</span>

        <span class="description text--gray-40">
          {{ genus }} &middot; {{ generation_label }}{% if is_legendary %} &middot; {{ t.legendary }}{% endif %}{% if is_mythical %} &middot; {{ t.mythical }}{% endif %}
        </span>

        <div class="flex flex--row gap--small">
          <span class="outline px--xlarge py--small" style="font-size:1rem; font-weight:600; letter-spacing:.05em; line-height:1.6;">{{ type_1 | upcase }}</span>
          {% if type_2 != "" %}
          <span class="outline px--xlarge py--small" style="font-size:1rem; font-weight:600; letter-spacing:.05em; line-height:1.6;">{{ type_2 | upcase }}</span>
          {% endif %}
        </div>

        {% if flavor != "" %}
        <span class="description" data-clamp="3">{{ flavor }}</span>
        {% endif %}

        <div class="border--h-25" style="height:2px;"></div>

        <div class="flex flex--row gap--xxlarge">
          <div class="flex flex--col">
            <span class="text--gray-40" style="font-size:1rem;">{{ t.height }}</span>
            <span style="font-size:3rem; font-weight:700; line-height:1;">{{ height_m }}<span style="font-size:1.5rem;"> m</span></span>
          </div>
          <div class="flex flex--col">
            <span class="text--gray-40" style="font-size:1rem;">{{ t.weight }}</span>
            <span style="font-size:3rem; font-weight:700; line-height:1;">{{ weight_kg }}<span style="font-size:1.5rem;"> kg</span></span>
          </div>
        </div>

      </div>
    </div>

    {% if has_evolution %}
    <div class="flex flex--col gap--xsmall" style="flex:0 0 auto;">
      <div class="border--h-25" style="height:2px;"></div>
      <div class="flex flex--row flex--center-x flex--center-y gap--medium">
        {% for stage in others %}
          {% if forloop.index0 > 0 %}
            <span class="text--gray-40" style="font-size:1.8rem;">&rarr;</span>
          {% endif %}
          {% if stage.marker %}
            <div class="flex flex--col flex--center-x flex--center-y">
              <span style="font-size:2.6rem; line-height:1;">&uarr;</span>
              <span class="text--gray-40 text--center" style="font-size:0.95rem;">{{ name }}</span>
            </div>
          {% else %}
            {% for m in stage.members %}
            <div class="flex flex--col flex--center-x" style="flex:1 1 0; min-width:0; max-width:210px;">
              {% if m.art != "" %}
              <img class="image image--contain w--full" src="{{ m.art }}" style="max-height:180px;" />
              {% endif %}
              <span class="text--center" style="font-size:1.05rem; line-height:1.15;">{{ m.name }}</span>
            </div>
            {% endfor %}
          {% endif %}
        {% endfor %}
      </div>
    </div>
    {% endif %}

  </div>
</div>